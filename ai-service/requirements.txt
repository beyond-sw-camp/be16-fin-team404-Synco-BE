# FastAPI STT Service - Kafka Consumer

# ASR (Speech Recognition)
faster-whisper==1.0.3
ctranslate2==4.6.0

# PyTorch (CPU 전용 - lighter weight)
torch --index-url https://download.pytorch.org/whl/cpu

# Kafka
kafka-python==2.0.2

# HTTP 클라이언트 (S3 파일 다운로드용)
requests==2.31.0

# AWS S3 SDK (선택적 - S3 직접 접근 시 필요)
boto3==1.34.0

# 환경변수
python-dotenv==1.0.0

# 유틸리티
numpy==1.24.3
